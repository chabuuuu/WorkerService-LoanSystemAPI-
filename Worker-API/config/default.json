{
    "RabbitMQ": {
      "connectionString": "amqp://localhost:5672"
    },
    "request-limit": {
        "limit": "100kb"
    },
    "json-compress": {
        "enable": false
    },
    "cors": {
        "origin":["localhost", "http://127.0.0.1:5500"],
        "methods": ["GET", "POST", "PUT", "DELETE", "UPDATE"],
        "allowedHeaders": ["X-Requested-With", "X-HTTP-Method-Override", "Content-Type", "Accept", "Observe"]
    }
}